@page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = "Register confirmation";
}

<h1>@ViewData["Title"]</h1>
@{
    if (@Model.DisplayConfirmAccountLink)
    {
<p>
    This app does not currently have a real email sender registered, see <a href="https://aka.ms/aspaccountconf">these docs</a> for how to configure a real email sender.
    Normally this would be emailed: <a id="confirm-link" href="@Model.EmailConfirmationUrl">Click here to confirm your account</a>
@Html.HiddenFor(model => model.Email, new { id = "email" });
</p>
    }
    else
    {
<p>
        Please check your email to confirm your account.
</p>
    }
}

@section Scripts {
    <script>
        $("#confirm-link").on("load", () => {
        });

        //$(document).ready(function () {
        //    $.post('../../api/UserEntities/insertEmail', {
        //        email: $('#email').val()  //User.Identity.Name does not exist in this context, so you have to use the registration model
        //    }).then(alert('hello'));
        //});
        $(document).ready(function () {
            var data = {
                email: $('#email').val()
            };
            $.ajax({
                type: "POST",
                url: '../../api/UserEntities/insertEmail',
                contentType: 'application/json',
                data: JSON.stringify(data),
                success: function () {
                    //...
                }
            });
        });
    </script>
}